//@ts-check
export const baseURL = window.location.origin
export class BaseService {
    baseURL = baseURL;

    /**
     * 
     * @param {Object} params 
     */
    getAll(params) {
        //...
    }
}